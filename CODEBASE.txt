# Codebase Map (kept current; edit when structure changes)

packages/
└── bt_common/
    ├── pyproject.toml
    ├── src/
    │   ├── config.py              # shared env settings (DATABASE_URL, Matrix, EMOS, etc.)
    │   ├── evermemos_client.py    # EverMemOS SDK wrapper
    │   ├── logging.py             # JSON logging + correlation IDs
    │   └── exceptions.py
    ├── tests/
    └── uv.lock

services/
├── agents_service/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── alembic.ini
│   ├── alembic/
│   │   ├── env.py
│   │   └── versions/
│   ├── src/
│   │   ├── server.py             # Litestar ASGI app (Synapse appservice endpoints)
│   │   ├── bootstrap.py          # local E2E provisioning CLI (SQLite-backed)
│   │   ├── agent/                # agent factory + tools + providers
│   │   ├── matrix/               # Matrix appservice handler + client
│   │   ├── models/               # Evidence/Citation + Segment domain models
│   │   ├── database/             # SQLAlchemy ORM + Store (SQLite local / Postgres prod)
│   │   └── voice/                # voice session orchestration + backends
│   └── tests/
├── ingestion_service/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── src/                      # ingestion pipelines + adapters
│   └── tests/
└── voice_call_service/
    ├── package.json
    └── src/                      # MatrixRTC/WebRTC sidecar

deploy/
└── local/
    ├── docker-compose.yml         # Synapse + Element Web
    ├── element/config.json
    ├── ingest/manifest.yaml
    ├── ghosts.yaml
    ├── bin/
    └── synapse/
